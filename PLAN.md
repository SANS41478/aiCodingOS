# 项目进度台账

> 这是项目当前状态的唯一事实源。每个任务必须有唯一 ID；状态变更必须同时更新证据。

## 当前状态摘要

- 当前阶段：Discover
- 正在做：无
- 阻塞：无
- 待决策：无
- 下一步：填写项目目标和第一批任务

## 状态定义

| 状态 | 含义 |
| --- | --- |
| TODO | 已登记，尚未开始 |
| DOING | 正在执行 |
| BLOCKED | 因明确阻塞条件无法继续 |
| DONE | 完成定义全部满足 |
| CANCELLED | 经确认取消，不再继续 |

## 阶段定义

| 阶段 | 进入条件 | 退出产物 |
| --- | --- | --- |
| Discover | 有待解决的问题或机会 | 事实、约束、未知项 |
| Define | 已掌握足够事实 | 目标、范围、验收标准 |
| Decide | 存在影响实现的选择 | 决策记录或确认无需决策 |
| Build | 目标和路径已明确 | 可演示的最小纵向切片 |
| Verify | 实现已完成 | 检查结果、人工验收、证据 |
| Close | 验收通过 | 台账、汇报、提交和后续项 |

## 决策队列

| ID | 决策 | 最晚决策点 | 状态 | 结果 / 备注 |
| --- | --- | --- | --- | --- |
| D-001 | 示例：选择持久化方式 | Define 结束前 | 待定 | 见 `docs/decisions/` |

## 任务清单

| ID | 阶段 | 任务 | 状态 | 验收标准 | 证据 | 等级 | 依赖 | 负责人 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| T-001 | Discover | 盘点项目目标、约束和已有资产 | TODO | 事实清单已记录；未知项已列出 | 待补 | standard | - | - | high |
| T-002 | Define | 定义第一个纵向切片 | TODO | 范围、非目标、验收路径已明确 | 待补 | standard | T-001 | - | high |
| T-003 | Decide | 完成首个高影响技术决策 | TODO | 候选方案和最终结论已记录 | 待补 | critical | T-002 | - | normal |
| T-004 | Build | 实现第一个可演示闭环 | TODO | 输入到输出链路可运行 | 待补 | standard | T-003 | - | high |
| T-005 | Verify | 执行检查并完成验收 | TODO | 自动检查和人工验收均通过 | 待补 | standard | T-004 | - | high |
| T-006 | Close | 更新台账并提交结果 | TODO | 证据包、汇报和后续项齐全 | 待补 | lite | T-005 | - | normal |
| T-007 | Close | 初始化 Git 仓库并提交推送 | DOING | 当前工作区已提交到 `main`，远端分支与本地提交一致 | 待补 | standard | - | codex | high |

## 阻塞项

| ID | 阻塞条件 | 影响任务 | 解除条件 | 负责人 |
| --- | --- | --- | --- | --- |
| - | 无 | - | - | - |

## 依赖规则

- 依赖必须引用已存在的任务 ID。
- 只有依赖任务全部 `DONE`，当前任务才会出现在 `ready` 列表。
- `BLOCKED` 或 `CANCELLED` 依赖不会自动改变下游任务状态，只会出现在 `blockers` 提示中。

## 示例任务闭环

下面的任务仅用于说明如何使用模板，实际项目开始后可替换或删除。

```text
T-EXAMPLE-01
Discover：确认用户需要“导出项目状态”
Define：只导出 JSON；不做云同步和定时导出
Decide：选择 JSON Lines 还是单个 JSON 文件，并记录 ADR
Build：实现按钮/命令 → 序列化 → 文件输出
Verify：合法数据、空数据、失败写入三种场景通过
Close：附测试结果、演示路径、提交号和后续事项
```



